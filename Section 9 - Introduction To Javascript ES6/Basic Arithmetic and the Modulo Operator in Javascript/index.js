// Remainder symbol = %

console.log(6 % 4);

// Remember the operator precedence in math
var cost = 3 + 5 * 2;
var cost2 = (3 + 5) * 2; // Using parentheses even if it's not necessary logically, it facilitates the redability of the code.

// Challenge
var dogAge = prompt("Type the dog's age: ");
var humanAge = ((dogAge - 2) * 4) + 21;

alert("A " + dogAge + " years old dog is equivalent to a " + humanAge + " years old human.");
