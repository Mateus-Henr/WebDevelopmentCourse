// Selecting elements using plain JS
document.querySelector("h1");

// Selecting elements using jQuery. It works similarly to using the "querySelectorAll()" method. It means that we can use classes, ids or HTML elements, and selects all of the
// elements on screen.
$("h1");
